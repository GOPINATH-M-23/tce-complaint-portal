{
  "name": "tce-smart-complaint-portal-root",
  "private": true,
  "version": "3.0.0",
  "type": "module",
  "scripts": {
    "dev:student": "npm --prefix student run dev",
    "dev:admin": "npm --prefix admin run dev",
    "build:student": "npm --prefix student run build",
    "build:admin": "npm --prefix admin run build",
    "build": "npm run build:student && npm run build:admin",
    "install:all": "npm install --prefix student && npm install --prefix admin"
  },
  "dependencies": {
    "firebase": "^10.12.2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
